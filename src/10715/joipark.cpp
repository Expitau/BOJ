#include <csdio>
#include <algorithm>
using namespace std;

int main(){
	int n, m, c;
	scanf("%d %d %d", &n, &m, &c);
	for(int i=0; i<m; i++){
		int a, b, d;
		scanf("%d %d %d", &a, &b, &d);
		
	}
	return 0;
}
